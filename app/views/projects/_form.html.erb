<%= form_for(@project) do |f| %>
	<div class="field">
		<%= f.label :title %>
		<%= f.text_field :title %>
	</div>

	<div class="field">
		<%= f.label :description %>
		<%= f.text_area :description %>
	</div>

	<div class="field">
		<%= f.label :end_time, "End on date & time" %>
		<%= f.date_field :end_time %>
	</div>

	<div class="action">
		<%= f.submit %>
	</div>

<% end %>